<package name="nvidia-legacy-@SERIES@" project="home:kami911:nvidia-legacy:modules">
  <title>NVIDIA legacy @SERIES@ driver (DKMS)</title>
  <description>Reproducible DKMS build of NVIDIA @VERSION@. Generated by CI.</description>
  <person userid="kami911" role="maintainer"/>

  <!-- CI toggles these two based on the no-GPU test gate for each repo/arch. -->
  <!-- @PUBLISH_BLOCK@ is replaced with <enable/> or <disable/> per repository. -->
@PUBLISH_BLOCK@

  <!-- best-effort / experimental series: keep off the default published repo -->
@BUILD_BLOCK@
</package>
